// Apply plugins using the legacy `apply` syntax for maximum compatibility
apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'
apply from: "$flutterRoot/packages/flutter_tools/gradle/flutter.gradle"
apply plugin: 'com.google.gms.google-services' // This must stay last

android {
    namespace = "com.example.detest_2405" // Your project's namespace
    
    // THIS IS THE MAIN FIX: Manually set the compile SDK version
    compileSdkVersion = 34 
    
    ndkVersion = flutter.ndkVersion

    // The compileOptions block is not needed and should be removed if it exists.

    kotlinOptions {
        jvmTarget = '1.8'
    }

    sourceSets {
        main.java.srcDirs += 'src/main/kotlin'
    }

    defaultConfig {
        applicationId = "com.example.detest_2405"
        minSdk = 21 // Ensure minSdk is set
        targetSdk = 34 // Target a modern SDK
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}

dependencies {
    // Add any specific Android dependencies here if needed
}
